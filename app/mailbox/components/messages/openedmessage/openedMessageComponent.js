import template from './openedMessage.html';
import controller from './openedMessageController';

let openedMsgComponent = {
    bindings: {
    },
    template,
    controller,
    controllerAs: 'msgOpen'
};

export default openedMsgComponent;