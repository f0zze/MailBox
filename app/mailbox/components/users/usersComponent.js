import template from './users.html';
import controller from './usersController';

let navComponent = {
    bindings: {},
    template,
    controller,
    controllerAs: 'users'
};

export default navComponent;